work/
__pycache__/

# Measurements are regenerated by ./run.py and ./sweep.py; keeping them in
# git made every later PR huge for no benefit. RESULTS.md and the per-file
# tables are the findings, so those stay.
results/*
!results/RESULTS.md
!results/PER-FILE-WINS*.md
